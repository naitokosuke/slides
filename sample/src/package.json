{
  "name": "src",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build --base /sample/ --out ../../dist/sample",
    "dev": "slidev --open",
    "export": "slidev export --dark --output ../sample.pdf"
  }
}
